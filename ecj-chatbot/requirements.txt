# EuGH Chatbot Dependencies
# Requires Python 3.11-3.13 (Python 3.14 not yet supported by chromadb)
#
# Install: pip install -r requirements.txt
# If using Python 3.14, switch to 3.13: py -3.13 -m pip install -r requirements.txt
#
# Verify your setup afterwards with: python diagnose.py

# Data Acquisition
SPARQLWrapper>=2.0.0
requests>=2.32.4
beautifulsoup4>=4.12.0
lxml>=6.1.1

# Vector Database & Embeddings
# chromadb pinned <1.0.0 to avoid CVE-2026-45829 (pre-auth RCE, CVSS 10.0, affects 1.0.0-1.5.8, unpatched at time of writing).
chromadb>=0.5.23,<1.0.0
# sentence-transformers CVE-2026-68770 (RCE via trust_remote_code bypass) has no confirmed patched version;
# pin to a recent release. Do not pass trust_remote_code=True in code.
sentence-transformers>=5.0.0

# LLM API
anthropic>=0.40.0

# Web Frontend
# streamlit >=1.54.0 clears CVE-2024-42474, CVE-2026-33682, CVE-2026-10804.
streamlit>=1.54.0

# Utilities
python-dotenv>=1.0.0
# tqdm >=4.66.3 fixes CVE-2024-34062 (CLI-arg eval RCE).
tqdm>=4.66.3
